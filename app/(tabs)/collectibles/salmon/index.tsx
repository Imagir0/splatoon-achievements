import { Text, View } from 'react-native';

export default function SalmonScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Liste des Salmon Run ici</Text>
    </View>
  );
}
